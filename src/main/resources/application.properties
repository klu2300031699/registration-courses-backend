spring.application.name=courses
server.port=${PORT:3593}

# Database configuration (uses environment variables in production)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/courses?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Gnanesh@1561}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
logging.level.org.springframework=INFO
logging.level.com.example.house=DEBUG

#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


